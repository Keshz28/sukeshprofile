# The site is exported to static files locally and committed in `out/`.
# Netlify just publishes them — no cloud build, no Next runtime needed.
[build]
  command = "echo 'Publishing prebuilt static export from out/'"
  publish = "out"
